# VidMap project website

Static project website for VidMap, published at https://cvg.github.io/vidmap/.

This branch contains deployment assets only. The reconstruction software is
maintained separately on `main`. Open `index.html` through a local HTTP server
to preview the site; preserve the relative asset directories when deploying.

Only delivery media is included. Higher-resolution masters and visual tuning
tools are retained locally and are not required to serve the website.
